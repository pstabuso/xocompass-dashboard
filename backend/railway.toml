[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn main:app -w 2 -k uvicorn.workers.UvicornWorker --timeout 1000 --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
